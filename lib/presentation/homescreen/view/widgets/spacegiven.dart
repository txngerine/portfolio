import 'package:flutter/material.dart';

class spacebargiven extends StatelessWidget {
  const spacebargiven({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50,
    );
  }
}
